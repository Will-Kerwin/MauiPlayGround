<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:TestMauiShell.Pages"
       xmlns:viewmodels="clr-namespace:TestMauiShell.ViewModels"
       x:DataType="viewmodels:ShellViewModel"
       x:Class="TestMauiShell.Pages.AppShell">
    <TabBar>
        <Tab Title="{Binding Home.Title}">
            <ShellContent Title="Title" ContentTemplate="{DataTemplate pages:HomePage}" />
            <ShellContent Title="Title Long" ContentTemplate="{DataTemplate pages:HomePage}" />
            <ShellContent Title="Title" ContentTemplate="{DataTemplate pages:HomePage}" />
            <ShellContent Title="Title" ContentTemplate="{DataTemplate pages:HomePage}" />
            <ShellContent Title="Title" ContentTemplate="{DataTemplate pages:HomePage}" />
            <ShellContent Title="Title" ContentTemplate="{DataTemplate pages:HomePage}" />
            <ShellContent Title="Title" ContentTemplate="{DataTemplate pages:HomePage}" />
            <ShellContent Title="Title" ContentTemplate="{DataTemplate pages:HomePage}" />
            <ShellContent Title="Title" ContentTemplate="{DataTemplate pages:HomePage}" />
        </Tab>
        <!--<Tab Title="{Binding Counter.Title}">
            <ShellContent ContentTemplate="{DataTemplate pages:CounterPage}" />
        </Tab>-->
    </TabBar>
</Shell>